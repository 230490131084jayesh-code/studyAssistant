fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pydantic==2.9.2
python-dotenv==1.0.1
anthropic==0.39.0
httpx==0.27.2

# Document parsing
pypdf==4.3.1
python-pptx==1.0.2
python-docx==1.1.2

# Local vector search (no external API key needed for retrieval)
# Note: sentence-transformers/PyTorch removed — using Chroma's built-in
# ONNX-based embedding function instead (see vector_store.py). Much lighter
# on memory, which matters on a 512MB free-tier instance.
chromadb==0.5.5
